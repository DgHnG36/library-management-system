apiVersion: v1
kind: Service
metadata:
  name: lms-order-service
  labels:
    app: order
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: order
  ports:
    - name: grpc
      protocol: TCP
      port: 40043
      targetPort: 40043
