apiVersion: v1
kind: Service
metadata:
  name: lms-user-service
  labels:
    app: user
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: user
  ports:
    - name: grpc
      protocol: TCP
      port: 40041
      targetPort: 40041
