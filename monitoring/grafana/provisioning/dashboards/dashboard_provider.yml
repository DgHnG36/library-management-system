apiVersion: 1

providers:
  - name: "LMS Monitoring"
    orgId: 1
    folder: "k6-lms-dashboards"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards
